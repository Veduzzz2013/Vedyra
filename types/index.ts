export type NotesPackage = { id: string; subject: string; progress: number; updated: string; };
export type DashboardStats = { streak: number; completedHours: number; savedPdfs: number; aiQueries: number; };
